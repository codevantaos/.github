# rename-map.yaml - CodeVantaOS Repository Rename Registry
# Updated: post-final-naming

# Phase 1 renames (completed in restructure)
phase1_renames:
  # Brand de-coupling renames (205+ -> 17 -> 7+1)
  # See restructure_final_report.md for full history

# Phase 2 renames (final naming standardization)
phase2_renames:
  - old: app-vercel
    new: app-portal
    reason: "Remove vendor brand (Vercel), describe domain responsibility"
    status: COMPLETED
    
  - old: mod-platform
    new: mod-suite
    reason: "Clarify as module collection, not platform itself"
    status: COMPLETED
    
  - old: gitops-agent
    new: infra-gitops-agent
    reason: "Add layer prefix for consistency (infra-*)"
    status: COMPLETED

# New repos (not renames)
new_repos:
  - name: app-ui
    reason: "Separate design system from portal app (app-portal depends on app-ui)"
    status: CREATED
    
  - name: ops-center
    reason: "Cross-repo control plane, distinct from ops-observability"
    status: CREATED

# Brand reference cleanup
brand_cleanup:
  indestructibleorg: 
    replaced_with: codevantaos
    status: COMPLETED (0 remaining)
  indestructible:
    replaced_with: codevantaos
    status: COMPLETED (0 remaining)
  softwareweb:
    replaced_with: codevantaos
    status: COMPLETED (0 remaining)
  axiom:
    replaced_with: codevantaos
    status: COMPLETED (0 remaining)

# Final repo list (10 repos)
final_repos:
  - .github
  - infra-base
  - infra-gitops-agent
  - core-main
  - core-kernel
  - app-portal
  - app-ui
  - mod-suite
  - ops-observability
  - ops-center
